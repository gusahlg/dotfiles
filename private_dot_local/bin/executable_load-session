#!/bin/sh

name="$1"

if [ -z "$name" ]; then
    echo "usage: load-session <session-name>"
    exit 1
fi

if tmux has-session -t "$name" 2>/dev/null; then
    exec tmux attach -t "$name"
else
    exec tmuxp load "$HOME/.tmuxp/$name.yaml"
fi
